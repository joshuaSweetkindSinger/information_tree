/*
This file defines class VistedNodeList, which is the toplevel dom element container on the
client side for the nodes that have been visited by the user.
 */
// ========================================================================
//                   Visited Node List
// ========================================================================
var VisitedNodeList = defCustomTag('visited-node-list', HTMLElement);

VisitedNodeList.prototype.init = function() {
  return this
};


